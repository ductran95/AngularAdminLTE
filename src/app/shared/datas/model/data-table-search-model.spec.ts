import { DataTableSearchModel } from './data-table-search-model';

describe('DataTableSearchModel', () => {
  it('should create an instance', () => {
    expect(new DataTableSearchModel()).toBeTruthy();
  });
});
