import { DataTableSearchParam } from './data-table-search-param';

describe('DataTableSearchParam', () => {
  it('should create an instance', () => {
    expect(new DataTableSearchParam()).toBeTruthy();
  });
});
