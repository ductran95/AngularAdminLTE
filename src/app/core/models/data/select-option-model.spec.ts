import { SelectOptionModel } from './select-option-model';

describe('SelectOptionModel', () => {
  it('should create an instance', () => {
    expect(new SelectOptionModel()).toBeTruthy();
  });
});
